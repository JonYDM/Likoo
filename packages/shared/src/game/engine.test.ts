import { describe, expect, it } from 'vitest';

describe('game engine', () => {
  // Placeholder de la Fase 0 — se reemplaza en la Fase 4 por los tests reales
  // de applyEvent() (ver docs/PLAN.md).
  it('el paquete está scaffolded y listo para la Fase 4', () => {
    expect(true).toBe(true);
  });
});
